@Test
    public void testReceiveWinnings() {
       Player p=new Player("Fred", 100);
       p.takeBet(5);
       p.receiveWinnings(5);
        assertEquals("This should be 105",p.getBalance(),105 );
    }
